TODO  Rewrite the danm readme
      and add all the shortcuts
      in a list, and where
      they are located in the files

TODO  Create a bash-file for mounting external drive in /opt/
      # FAT32
      sudo mount -t vfat /dev/sdb1 /media/external -o uid=1000,gid=1000,utf8,dmask=027,fmask=137
      # NTFS
      sudo mount -t ntfs-3g /dev/sdb1 /media/external

      # UNMOUNT
      sudo umount /dev/sdb1
      sudo umount /media/external

TODO  bindsym Print exec ~/bin/mlscreenshot.sh
      using scrot

Syntax:
  http://daringfireball.net/projects/markdown/syntax
